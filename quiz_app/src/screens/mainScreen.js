import React from 'react';
import styled from 'styled-components';

const  MainScreen = () => {
    return (
        <MainBox>Main Screen</MainBox>
    )
}

const MainBox = styled.div`
    width: 100%;
    height: 100vh;
    background-color: red;
`

export default MainScreen;